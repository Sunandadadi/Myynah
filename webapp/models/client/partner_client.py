class PartnerClient(object):

    def fetch_ebay_results():
        url = self.construct_url()

    def construct_url(self):
        return
        "&SERVICE-VERSION=1.0.0 \
        &SECURITY-APPNAME={} \
        &RESPONSE-DATA-FORMAT=JSON \
        &REST-PAYLOAD \
        &keywords={} \
        &SortOrderType=BestMatch \
        &paginationInput.entriesPerPage=2".format()
